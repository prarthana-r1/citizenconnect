// export async function POST(req) {
//     const { userId, schemeId } = await req.json();
//     await User.findByIdAndUpdate(userId, { $push: { savedSchemes: schemeId } });
//     return NextResponse.json({ message: "Scheme saved!" });
//   }
  